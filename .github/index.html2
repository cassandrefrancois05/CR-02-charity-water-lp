<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Charity:Water</title>
  <style>
    /* Reset some default spacing */
    body, h1, h2, p {
      margin: 0;
      padding: 0;
    }

    body {
      font-family: "Proxima Nova", "Avenir", Arial, sans-serif;
      line-height: 1.6;
      color: #111;
      background-color: #fff;
    }

    /* Header */
    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 10%;
      background: #fff;
      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
    }

    header .logo {
      font-weight: bold;
      font-size: 1.2rem;
      color: #FFC907; /* Primary Yellow */
    }

    nav a {
      margin-left: 20px;
      text-decoration: none;
      color: #2E9DF7; /* Primary Blue */
      font-size: 0.95rem;
      font-weight: 500;
    }

    nav a:hover {
      color: #159A48; /* Dark Green secondary */
    }

    /* Hero Section */
    .hero {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      min-height: 80vh;
    }

    .hero-text {
      flex: 1;
      background: #2E9DF7; /* Blue background */
      color: #fff;
      padding: 40px;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .hero-text h1 {
      font-size: 2.4rem;
      margin-bottom: 20px;
      font-weight: 700;
    }

    .hero-text p {
      font-size: 1rem;
      margin-bottom: 30px;
      line-height: 1.5;
      max-width: 500px;
    }

    .hero-text .cta-btn {
      display: inline-block;
      padding: 12px 22px;
      background: #FFC907; /* Yellow CTA */
      color: #111;
      font-weight: bold;
      border-radius: 8px;
      text-decoration: none;
      transition: 0.3s ease;
    }

    .hero-text .cta-btn:hover {
      background: #FF902A; /* Orange hover */
      color: #fff;
    }

    .hero-image {
      flex: 1;
      min-height: 400px;
      background: url("https://via.placeholder.com/800x600") no-repeat center center/cover;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .hero {
        flex-direction: column;
      }

      .hero-text, .hero-image {
        flex: none;
        width: 100%;
      }
    } /* Responsive Tweaks */ 
    /* Tablet (screens smaller than 768px) */
    @media (max-width: 768px) {
      .hero {
    flex-direction: column; /* stack text and image */
    }
    .hero-text {
    padding: 30px 20px;
    text-align: center;
      }
      
        .hero-text h1 {
    font-size: 2rem;
    }
    
     nav a {
    margin-left: 10px;
    font-size: 0.9rem;
     }
    }
    /* Mobile (screens smaller than 480px) */
    @media (max-width: 480px) {
      header {
         flex-direction: column; /* logo on top, nav under it */
          padding: 10px;
        }
        nav {
          margin-top: 10px;
        }
        
        .hero-text h1 {
           font-size: 1.6rem;
        }
        
        .hero-text p {
          font-size: 0.9rem;
        }
        .hero-text .cta-btn {
          padding: 10px 16px;
           font-size: 0.9rem;
        }
    }
  </style>
</head>
<body>

  <!-- Header -->
  <header>
    <div class="logo">Charity:Water</div>
    <nav>
      <a href="#">About Us</a>
      <a href="#">Hear Their Stories</a>
      <a href="#">Give</a>
    </nav>
  </header>

  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-text">
      <h1>Bringing Clean Water, Seeing the Impact</h1>
      <p>In a few taps, 100% of your donation brings safe, clean water to families, opening the door to opportunity. Watch their lives transform — and see exactly how your gift made it possible.</p>
      <a href="#" class="cta-btn">Tap to Change a Life</a>
    </div>
    <div class="hero-image"></div>
  </section>

</body>
</html>
